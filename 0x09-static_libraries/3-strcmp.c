#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @*s1: pointer to string to compare
 * @s1: string to comapre to
 * @*s2: pointer to string to compare to
 * @s2: string to compare
 * Return: an integer value
 */
int _strcmp(char *s1, char *s2)
{
int a;
a = strcmp(s1, s2);
return (a);
}
