#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("size of int:%d", size of(int)\n);
printf("size of char:%c", size of(char)\n);
printf("size of long int:%ld", size of(long int)\n);
printf("size of long long int:%lid", size of(long long int)\n);
printf("size of float:%f", size of(float)\n);

	return (0);
}
