#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/*
 * _strlen_recursion - prints length of string
 *
 * Return: void
 */
int _strlen_recursion(char *s)
{
int len;
len = strlen(s);
return (len);
}
