#include "main.h"
#include <stdio.h>
int is_prime_number(int n)
{
int q;
if (n == 1)
{
return (0);
}
else if (n % q == && q != n && q != 1)
{
return (0);
}
else if (n %q == 0 && q == n)
{
return (1);
}
else
return (is_prime_number(q + 1, n);
}
