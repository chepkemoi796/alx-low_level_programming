#include <stdio.h>
#include <stdlib.h>
/**
 * main - adds positive numbers
 * @argc: argument number
 * @argv: arguments
 * Return: (0)
 */
int main(int argc, char *argv[])
{
int i, sum = 0;
if (argc <= 0)
printf("0\n");
for (i = 0; i < argc; i++)
{

}
}
printf("%d\n", sum);
return (0);
}
