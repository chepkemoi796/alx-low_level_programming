#include "main.h"
#include <stdio.h>
#include <stdlib.h>
void *_calloc(unsigned int nmemb, unsigned int size)
{
char *a;
a = calloc(nmemb * sizeof(size * char))
if (nmemb == 0)
{
return (NULL);
}
if (size == 0)
{
return (NULL);
}
}
